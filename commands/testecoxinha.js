module.exports = {
    name: 'testecoxinha',
    description: 'teste',
    usage: 'teste',
    execute(message, args) {
        //if(!args) return;
        //var user = message.mentions.users.first().id;
        //message.channel.send("Testando " + "<@" + user + ">");

        /*
        const filter = m => m.author.id === message.author.id;
        message.channel.awaitMessages(filter, {max: 5, time: 10000}).then(collected => {
            console.log(collected.find(y => y === 'yes'));
        }).catch(err => {
            console.log('Erro no duelo');
            console.log(err);
        })
        */
    }
}

